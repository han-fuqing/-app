# 旧时光中古市集

一个可直接部署到 Cloudflare Pages 的静态中古商品网页。

## 已有功能

- 商品列表与分类筛选
- 关键词搜索
- 本地收藏
- 发布闲置演示
- 手机与桌面端自适应

## 本地运行

无需安装依赖。在当前目录执行：

```powershell
python -m http.server 4173
```

然后访问 `http://127.0.0.1:4173`。

## Cloudflare Pages 部署

这是纯静态站点，无需构建命令。部署时将输出目录设置为 `/`（仓库根目录）即可。
